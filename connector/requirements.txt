# Fivetran's runtime pre-installs the fivetran_connector_sdk and requests
# packages for every deployed connector -- do not list either here, or
# deployment can fail with a version conflict. Add only extra third-party
# dependencies the connector code itself imports.
#
# Empty for now -- this connector needs nothing beyond `requests` (already
# provided). NOTE: `fivetran debug` runs `pipreqs` internally and appears
# to delete/regenerate this file as a side effect of that step -- if it's
# missing after a local debug run, that's why; restore this committed
# version rather than trusting whatever pipreqs auto-generates locally.
